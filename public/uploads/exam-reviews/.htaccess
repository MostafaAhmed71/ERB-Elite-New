# CORS لملفات PDF (مطلوب للتحميل المباشر من الواجهة)
<IfModule mod_headers.c>
  Header set Access-Control-Allow-Origin "*"
  Header set Access-Control-Allow-Methods "GET, OPTIONS"
  Header set Access-Control-Allow-Headers "*"
</IfModule>

Options -Indexes
